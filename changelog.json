[
  {
    "version": "v1.1.7",
    "date": "18/08/2026",
    "title": "Cleaner Script Error Traces",
    "changes": {
      "Fixed": [
        "Pre-request and post-response script errors no longer show a raw, unhelpful engine stack trace (e.g. \"at new AsyncFunction (<anonymous>)... at self.onmessage...\"). Errors are now trimmed down to the message plus a clean trace through the user's own script functions, with correct line/column numbers and a \"Pre-request script —\" / \"Post-response script —\" prefix identifying which phase raised it.",
        "The bundled skill.md now documents that pre_script and post_script are each allowed at most once per request section (independently of each other) — one of each is the normal shape. Previously undocumented, which could lead an AI agent to insert a second pre_script or post_script instead of adding more lines to the existing one.",
        "Fixed Node.js/Python detection reporting \"not found\" on machines where they were genuinely installed. The detector previously only checked a short hardcoded list of common install paths (including a `~/.nvm/current` symlink that nvm does not actually create by default) against the minimal PATH Electron GUI apps inherit when launched from Finder/Dock/Taskbar — invisible to anything installed via nvm, asdf, mise, pyenv, or a non-default location. Detection now first resolves the PATH exactly as the user's own login shell would see it (sourcing .zshrc/.bash_profile/nvm.sh, etc.), falls back to correctly reading nvm's actual on-disk layout, and finally the existing hardcoded paths. Also fixed Python's detector silently having no PATH fallback at all (unlike Node's), and scripts now run with the same resolved PATH rather than a narrower hardcoded one.",
        "Fixed the skill.md never documenting how to actually use a voiden.variables.set() value outside a script. It showed the get/set API for reading/writing a variable from inside another script, but never once showed that referencing it in a plain header/URL/body field elsewhere requires {{process.KEY}} template syntax specifically — a bare {{KEY}} silently never resolves (it persists to the same .voiden/.process.env.json the runtime-variables block writes to, read back by a resolver that only matches process.-prefixed expressions). An agent that correctly set a variable in a post_script had no way to know how to reference it in a later request without guessing, which is a very plausible root cause of the earlier \"agent cannot set runtime variables\" report.",
        "voiden.assert is the correct spelling for Python too, not voiden.assert_ — the skill.md and the script validator both had this backwards. The runtime's Python wrapper rewrites voiden.assert( to its internal voiden.assert_( right before execution (assert is a reserved keyword in Python, so .assert can't be written as a literal attribute access), but that rewrite is invisible plumbing never meant to be typed by hand. voiden.assert(...) is now the one documented and lint-recognized spelling across JavaScript, Python, and shell alike. \"Unknown function\" stays a warning, not a hard error, for both languages."
      ],
      "Changed": [
        "The script-assertion results panel's Copy button now shows a checkmark for ~1.2s after copying, instead of no feedback at all."
      ]
    }
  },
  {
    "version": "v1.1.6",
    "date": "29/07/2026",
    "title": "Inline Block Help",
    "changes": {
      "Added": [
        "The Pre-Request Script and Post-Response Script blocks now show an inline \"?\" help tooltip in their header, matching the other request blocks"
      ]
    }
  },
  {
    "version": "v1.1.5",
    "date": "16/07/2026",
    "title": "Block Documentation Link",
    "changes": {
      "Added": [
        "An \"Open Documentation\" item now appears in the right-click block menu for Pre-Request Script and Post-Response Script blocks, opening the Voiden Blocks docs in your system browser"
      ]
    }
  },
  {
    "version": "v1.1.2",
    "date": "03/07/2026",
    "title": "Skill Documentation Update",
    "changes": {
      "Improved": [
        "AI skill documentation updated with accurate scripting API reference for JavaScript and Python, including cookies access, correct read-only labels, and !== operator"
      ]
    }
  },
  {
    "version": "v1.1.4",
    "date": "10/06/2026",
    "title": "JSON Body Auto-Parse",
    "changes": {
      "Added": [
        "voiden.response.body is now auto-parsed for JSON responses — access keys directly (e.g. voiden.response.body.userId) in JavaScript and Python without manual JSON.parse()"
      ],
      "Fixed": [
        "Shell scripts continue to receive response body as a raw JSON string (use jq for key extraction)"
      ]
    }
  },
  {
    "version": "v1.1.3",
    "date": "29/05/2026",
    "title": "Build Fix",
    "changes": {
      "Fixed": [
        "Remove catch-all shim from build pipeline for correct dependency bundling"
      ]
    }
  },
  {
    "version": "v1.1.1",
    "date": "27/05/2026",
    "title": "Shim & Docs Fix",
    "changes": {
      "Fixed": [
        "Removed catch-all shim block",
        "Skill documentation rewritten with accurate API reference for JavaScript, Python, and Shell"
      ]
    }
  },
  {
    "version": "v1.1.0",
    "date": "20/04/2026",
    "title": "Python & Shell Support",
    "changes": {
      "Added": [
        "Python script execution via post-response hooks",
        "Shell/bash script support",
        "voiden.setVariable() and voiden.getVariable() cross-script state"
      ]
    }
  },
  {
    "version": "v1.0.0",
    "date": "01/04/2026",
    "title": "Initial Release",
    "description": "Post-response JavaScript scripting for Voiden.",
    "changes": {
      "Added": [
        "JavaScript post-response script execution",
        "Access to response status, headers, and body",
        "Runtime variable setting via scripts",
        "Console output in response panel"
      ]
    }
  }
]
